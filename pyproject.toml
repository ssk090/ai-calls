[project]
name = "ai-calls"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shivananda Sai", email = "shivananda.sai@altir.co" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "groq>=1.7.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]

[project.scripts]
ai-calls = "ai_calls:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
